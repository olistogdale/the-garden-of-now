'use strict';

import {mongoose} from '../../database/db';

import type {SeasonalIngredientFallbackDatabase} from '../../../../data/ingredients/types/ingredientTypes'

const {Schema, model} = mongoose;